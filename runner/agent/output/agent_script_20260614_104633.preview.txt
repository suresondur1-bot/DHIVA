# Goal: Create a new stock correction document ,first select the store with code as 157-IP1 and name as IP-Pharmacy-Raipur then you enter into New correction document, enter the item code  KI-RA-N-0901-PH and select the item that comes ,then for that item select the batch  KILLA12,enetr the same in other dropdown,change the MRP current to 51 set the quantity to 50 then click on save
# Start: https://sqa.narayanahealth.org/phrweb/stock-correction-new
# Steps: 4

  1. search_select  #storeInput   # Store field shows IP-Pharmacy-Raipur suggestion selected
  2. search_select  #storeInput   # Store suggestion selected and Continue button enabled
  3. search_select  #storeInput   # Store suggestion selected and Continue button enabled
  4. search_select  #storeInput   # Store suggestion selected and Continue button enabled
