/**
 * DEPRECATED — Visual testing is now handled via visual_checkpoint and
 * visual_figma_check step keywords inside normal ATHMA test cases (runner.py).
 * This server (port 6002) is no longer needed or started.
 * start_visual_testing.bat can be left stopped.
 */
