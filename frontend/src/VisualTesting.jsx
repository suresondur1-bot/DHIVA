// DEPRECATED — Visual testing is now handled via visual_checkpoint and
// visual_figma_check step keywords inside normal ATHMA test cases.
// This file is kept to avoid import errors but is no longer used.
// The nav item and import in app.jsx have been removed.
export function VisualTesting() { return null; }
